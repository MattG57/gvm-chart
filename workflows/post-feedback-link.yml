name: 'PR Survey Comment'
description: 'Posts a PR comment with a survey link'
inputs:
  base-url:
    description: 'Base survey URL'
    required: true

runs:
  using: 'composite'
  steps:
    - name: Comment
      uses: peter-evans/create-or-update-comment@v4
      with:
        token: ${{ github.token }}
        issue-number: ${{ github.event.pull_request.number }}
        body: |
          🧠 [Click to give feedback](${{ inputs.base-url }}?author=${{ github.event.pull_request.user.login }}&repo=${{ github.repository }}&prno=${{ github.event.pull_request.number }})
